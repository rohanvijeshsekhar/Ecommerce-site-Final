'use client';
import { ComingSoon } from '@/admin/pages/NotFound';

export default function AuditAdminPage() {
  return <ComingSoon module="Security & Audit Logs" description="Real-time audit log viewer for logins, permissions, catalog edits, and security events." />;
}
